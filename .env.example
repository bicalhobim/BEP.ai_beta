# ─────────────────────────────────────────────────────────────────────────
# BEP.ai — variáveis de ambiente
#
# O app roda 100% no NotebookLM. NÃO há chave de IA por env: a autenticação é
# feita pelo CLI/ponte do NotebookLM (login pelo próprio app). Este arquivo fica
# sem variáveis obrigatórias — mantido para futuras configs.
# ─────────────────────────────────────────────────────────────────────────
